import { GeneralApiProblem } from '@services/api';
import { <%= props.pascalCaseName %> } from '@models';

export type Get<%= props.pascalCaseName %>sResult =
  | { kind: "ok"; <%= props.camelCaseName %>s: <%= props.pascalCaseName %>[] }
  | GeneralApiProblem;

export type Get<%= props.pascalCaseName %>Result =
  | { kind: "ok"; <%= props.camelCaseName %>: <%= props.pascalCaseName %> }
  | GeneralApiProblem;

export type Post<%= props.pascalCaseName %>Result =
  | { kind: "ok"; <%= props.camelCaseName %>: <%= props.pascalCaseName %> }
  | GeneralApiProblem;

export type Update<%= props.pascalCaseName %>Result =
  | { kind: "ok"; <%= props.camelCaseName %>: <%= props.pascalCaseName %> }
  | GeneralApiProblem;

export type Delete<%= props.pascalCaseName %>Result =
  | { kind: "ok"; }
  | GeneralApiProblem;
